#include<stdio.h>
/**
 * main - Prints set expression
 *
 * Return: always 0 for succesful run
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
